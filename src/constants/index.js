/* Vivify Ideas - Project constants */

const API_ROUTE = '/api/'

const BACKEND_HOSTNAMES = {
  dev: 'http://localhost'
}

const ENVIRONMENTS = {
  localhost: 'dev'
}

const CLIENT_ID = 2

export {
  API_ROUTE,
  BACKEND_HOSTNAMES,
  ENVIRONMENTS,
  CLIENT_ID
}
